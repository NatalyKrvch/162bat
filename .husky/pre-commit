echo "🛡️ Running pre-commit checks..."

corepack yarn lint-staged || {
  echo "❌ Lint or format issues found. Commit aborted.";
  exit 1;
}

echo "✅ All staged files passed checks. Proceeding with commit."
